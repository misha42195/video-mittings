# Шаги выполненной работы

1. **Переинициализация git-репозитория.** Старая директория `.git` удалена (`rm -rf .git`), репозиторий создан заново командой `git init -b main` — история предыдущего коммита не сохранена (remote отсутствовал, потерь не было).
2. **Стейджинг файлов проекта.** Все файлы рабочего каталога (скелет монорепозитория `apps/web` + `apps/api`, конфиги, skills) добавлены в индекс командой `git add -A`.
3. **Проверка на секреты.** Список застейдженных файлов проверен вручную — конфиденциальных данных (.env, ключей и т.п.) не обнаружено.
4. **Создание коммита.** Создан коммит `chore: initial commit of web/api monorepo scaffold` (132 файла, 25308 добавленных строк).
5. **Определение технологий для модуля регистрации/авторизации.** Перед реализацией согласованы (уточняющими вопросами пользователю) и применены следующие технологии, с обоснованием:
   - **Вид тестов — API e2e (`pytest` + `httpx.AsyncClient`)**, а не браузерный Playwright: у auth-логики нет UI, тесты гоняются напрямую против ASGI-приложения (`ASGITransport`) и реального Postgres из `docker-compose.yml` (без моков БД) — это и есть цель e2e для backend-фичи.
   - **SQLAlchemy 2.0 (async, `DeclarativeBase`/`Mapped`/`mapped_column`) + `asyncpg`** — стандартный async ORM/драйвер для FastAPI, БД уже поднята через `docker-compose.yml` (Postgres 17).
   - **`bcrypt`** для хеширования паролей напрямую (без `passlib`, так как тот не поддерживается активно).
   - **`pyjwt`** для выпуска JWT (вместо `python-jose`, который менее активно поддерживается).
   - **`pydantic-settings`** для конфигурации через `.env` (`DATABASE_URL`, `JWT_SECRET_KEY` и т.д.) — стандартный подход в FastAPI-проектах.
   - **Подход разработки — TDD**: сначала написаны падающие e2e-тесты по сценарию (регистрация → JWT, логин по email+паролю → JWT, ошибочные случаи), затем — реализация, чтобы тесты стали проходить.
   - **Архитектура модуля — CQRS** (по отдельному запросу пользователя): `commands.py` (`RegisterUserCommand`/`RegisterUserHandler` — запись) отделён от `queries.py` (`AuthenticateUserQuery`/`AuthenticateUserHandler` — чтение, логин не меняет состояние БД), роутер (`routers/auth.py`) остался тонким и переводит доменные исключения (`exceptions.py`) в HTTP-ответы. Это дало возможность тестировать бизнес-логику отдельно от HTTP-слоя (`tests/test_auth_handlers.py`), не переусложняя при этом остальной проект (без отдельных БД на чтение/запись, без event sourcing — это было бы избыточно для одного auth-модуля).
6. **Установка зависимостей, фиксация версий и запуск проекта.** `uv sync` в `apps/api` (61 пакет, окружение уже было синхронизировано — новых изменений в `uv.lock` не было) и `npm install` в корне (380 пакетов, `package-lock.json` не изменился) — версии зафиксированы соответствующими lock-файлами и закоммичены ранее вместе с фичей. Поднят `docker-compose` (postgres, уже был healthy). Запущен `npm run dev` (веб на :3000 через Turbopack, API на :8000 через `fastapi dev` с автоперезагрузкой) в фоне. Смоук-тест вживую: `GET /health` → 200, `GET /` (web) → 200, `POST /auth/register` → 201 с JWT; тестовая запись пользователя удалена из БД после проверки.
7. **Создан `RUN.md`.** По просьбе пользователя рядом с `STEPS.md` добавлен файл-инструкция с командами запуска проекта: поднятие postgres, установка зависимостей, настройка `.env`, `npm run dev` (и раздельные `dev:web`/`dev:api`), способы проверки работоспособности (автотесты, Swagger UI, curl), а также справка для IDE (PyCharm Sources Root на `apps/api/src`, интерпретатор, что не нужно маркировать для `apps/web`).
8. **Написаны падающие (red) e2e-тесты для модуля встреч (`tests/test_meetings_e2e.py`)** — реализация роутеров/модели `Meeting` вынесена в отдельную задачу, на этом шаге только тесты, по TDD-подходу модуля auth. Перед написанием уточнены у пользователя (`AskUserQuestion`) три момента, определяющие форму API: (1) эндпоинты `/meetings` требуют JWT, как `/auth/*`; (2) у встречи, помимо `title`, есть `description` и `scheduled_at`; (3) `GET /meetings` возвращает только встречи текущего пользователя. Сценарии: `POST /meetings` создаёт встречу (201 + тело; 401 без токена; 422 без `title`); `GET /meetings` отдаёт список (пустой список для нового пользователя; только свои встречи, чужие не видны; 401 без токена); `GET /meetings/{id}` отдаёт встречу по id (200 + тело; 401 без токена; 404 для несуществующего id; 404 для чужой встречи — вытекает из требования «только свои»). Для авторизованных запросов в `tests/conftest.py` добавлены фикстуры `register_user` (фабрика: регистрирует+логинит нового пользователя, возвращает `Authorization`-заголовок) и `auth_headers` (готовый заголовок для одного пользователя) — переиспользуют уже реализованные `/auth/register` и `/auth/login`. Прогон подтвердил красный статус: 10 из 11 новых тестов падают (роутера `/meetings` ещё нет); один тест (`test_get_nonexistent_meeting_returns_404`) проходит уже сейчас — совпадение, так как FastAPI и без роутера отвечает 404 на любой незарегистрированный путь, но тест сохранён как значимая проверка на будущее. Все 13 ранее существовавших auth-тестов остались зелёными.
9. **Реализованы модель и роутер встреч, red-тесты стали зелёными.** По той же CQRS-архитектуре, что и auth: `models.py` — `Meeting` (`owner_id` FK на `users.id`); `schemas.py` — `MeetingCreate`/`MeetingResponse`; `exceptions.py` — `MeetingNotFoundError`; `commands.py` — `CreateMeetingCommand`/`Handler` (запись); `queries.py` — `ListMeetingsQuery`/`GetMeetingQuery` + хендлеры (чтение, оба скопированы по `owner_id` — пользователь видит только свои встречи; `GetMeetingHandler` бросает `MeetingNotFoundError` как для несуществующего id, так и для чужой встречи — 404 в обоих случаях, без утечки через 403). Добавлена `security.decode_access_token` и новый `dependencies.py` с `get_current_user`/`CurrentUser` (`OAuth2PasswordBearer`, декодирует JWT, подгружает `User`, 401 при отсутствии/невалидности токена или удалённом пользователе) — переиспользуемая зависимость для любых защищённых роутеров, не только `/meetings`. `routers/meetings.py` — тонкий HTTP-слой (`POST /meetings`, `GET /meetings`, `GET /meetings/{id}`), подключён в `main.py`. Прогон: все 23 теста (13 auth + 10 meetings) зелёные; `ruff check`, `ruff format --check`, `mypy src` — чисто. Обновлена документация: `apps/api/CLAUDE.md` (новые модули, назначение `dependencies.py`, поведение `GetMeetingHandler`, тестовые фикстуры `register_user`/`auth_headers`).
10. **Задокументирован паттерн CQRS.** В `apps/api/CLAUDE.md` добавлен раздел «CQRS pattern» между `## Architecture` и `## Tests`: структура команда/хендлер и запрос/хендлер (frozen-датаклассы `<Verb><Noun>Command`/`Query` + `<Name>Handler(db: AsyncSession)` с единственным `handle()`), разделение файлов `commands.py`/`queries.py`, правило «роутер — единственный слой, знающий про HTTP» (маппинг доменных исключений из `exceptions.py` в `HTTPException`), и явные конвенции для новых use case (именование, куда класть write/read, где заводить исключения, обязательный скоуп по `owner_id` в самом query/command). Раздел обобщает то, что уже было реализовано в auth- и meetings-модулях, без изменений в коде.
11. **Страница регистрации (`apps/web/src/app/register/page.tsx`) на HeroUI, подключённая к `POST /auth/register`.** Форма (email + пароль, без лишних полей — ровно то, что принимает API) собрана из компонентов HeroUI v3 (`Card` + `Form` + `TextField` + `Button` + `Alert`, композиционный API, без `HeroUIProvider` — в v3 он не нужен), с декоративным градиентным фоном. Клиентская валидация дублирует серверную (`EmailStr`, пароль ≥ 8 символов), 409 (email уже занят) показывается инлайн под полем email, остальные ошибки API/сети — баннером `Alert`. Добавлен `src/lib/api.ts` — типизированный fetch-клиент (`registerUser`, класс `ApiError` с полем `status`), base URL берётся из `NEXT_PUBLIC_API_URL` (пример — новый `apps/web/.env.example`, по умолчанию `http://localhost:8000`). При успехе JWT кладётся в `localStorage` (`access_token`), пользователь редиректится на `/`. Обнаружено и исправлено попутно: (1) в `apps/api` не было CORS — добавлены `cors_allow_origins` в `config.py` (default `["http://localhost:3000"]`) и `CORSMiddleware` в `main.py`, иначе браузер блокировал бы запросы к API; (2) `node_modules` был в сломанном/неполном состоянии (частично применённый `npm install` из-за незакоммиченного `package-lock.json`) — конкретно `@types/react` был захостен не рядом с `react`, из-за чего `moduleResolution: "bundler"` не находил типы `react` изнутри `react-aria-components`, и все компоненты HeroUI типизировались как `any` (диагностировано через `tsc --noEmit` с временно выключенным `skipLibCheck`); исправлено полным `rm -rf node_modules package-lock.json && npm install`. Проверено вживую через Playwright (браузер): успешная регистрация → JWT в `localStorage` → редирект на `/`; повторная регистрация тем же email → инлайн-ошибка «Пользователь с таким email уже зарегистрирован» без ошибок в консоли, кроме ожидаемого 409 от fetch. `tsc --noEmit`, `eslint`, `ruff check`, `mypy src` — чисто. Обновлены `apps/web/CLAUDE.md` (новые `src/lib/api.ts` и `src/app/register/`, HeroUI-конвенция) и `apps/api/CLAUDE.md` (поле `cors_allow_origins`, `CORSMiddleware` в `main.py`).
12. **UI/UX-ревью страницы регистрации** (`apps/web/src/app/register/page.tsx`), скилл `ui-ux-pro-max`: проверены домены `ux` (валидация форм/accessibility), `react` (стек), `product`/`icons` (паттерны auth-форм), а также `@heroui/react`/`@heroui/styles` в `node_modules` (нет встроенного toggle показа пароля, `--color-accent` — валидный тег темы HeroUI, не баг). Найдено: (1) нет кнопки «показать пароль» — у поля пароля нет способа проверить ввод перед сабмитом; (2) не указаны `autoComplete="email"`/`autoComplete="new-password"` — не помогает автозаполнению/менеджерам паролей браузера; (3) нет ссылки «уже есть аккаунт? войти» — ожидаемо, так как страницы `/login` пока не существует; (4) `apps/web/src/app/globals.css` жёстко задаёт `font-family: Arial, Helvetica, sans-serif` на `body`, хотя тема (`@theme inline`) настраивает `--font-sans` на Geist — токен объявлен, но не используется, шрифт не Geist на всём сайте, не только на этой странице. Само найденное — правки в код не вносились, ждём решения пользователя, что чинить.
13. **Исправлены пункты 1, 2, 4 UI/UX-ревью; пункт 3 отложен по решению пользователя.** (1) Добавлен тоггл видимости пароля: поле пароля обёрнуто в `InputGroup` (`@heroui/react`) с `InputGroup.Suffix` → `ToggleButton` (`isIconOnly`, `variant="ghost"`, `size="sm"`), переключающий `type` у `TextField` между `password`/`text`; иконки `EyeIcon`/`EyeOffIcon` — инлайн SVG в `page.tsx` (без новой зависимости — библиотеки иконок в проекте нет, а Phosphor пришлось бы ставить отдельно). (2) `autoComplete="email"` на email `Input`, `autoComplete="new-password"` на `InputGroup.Input` пароля. (4) `apps/web/src/app/globals.css`: `font-family: var(--font-sans), Arial, Helvetica, sans-serif` вместо жёсткого `Arial, Helvetica, sans-serif` — теперь подключается Geist (уже загружался в `layout.tsx`, но не был задействован). (3) — ссылка «уже есть аккаунт? войти» пропущена: пользователю предложены варианты (создать `/login` целиком / добавить мёртвую ссылку / пропустить), выбран вариант «пропустить», так как создание полноценной страницы логина — отдельная фича вне скоупа UI-фикса. Проверено вживую через Playwright: `tsc --noEmit` и `eslint` чисто; на странице `/register` — клик по тогглу меняет `type` инпута с `password` на `text` и обратно, иконка глаза меняется на перечёркнутую, `aria-pressed`/`aria-label` корректны; `input[name=email].autocomplete === "email"`, `input[name=password].autocomplete === "new-password"`; `getComputedStyle(document.body).fontFamily` → `"Geist, \"Geist Fallback\", Arial, Helvetica, sans-serif"`. Тестовые скриншоты и `.playwright-mcp/` за собой удалены, в рабочем дереве не осталось.
14. **Ограничение символов пароля и усиление проверки email на странице регистрации.** По запросу пользователя (уточнено через `AskUserQuestion`: набор символов — латиница + цифры + стандартные спецсимволы; поведение — блокировать ввод недопустимого символа; email — усилить текущий regex): (1) `EMAIL_PATTERN` заменён на паттерн с корректными правилами доменных меток (без ведущего/конечного дефиса, 1–63 символа на метку, обязательна хотя бы одна точка) и более широким набором символов локальной части (`!#$%&'*+/=?^_\`{|}~` в дополнение к прежним); (2) для пароля добавлен `PASSWORD_DISALLOWED_CHARS = /[^\x21-\x7E]/g` (весь печатаемый ASCII, кроме пробела, — то есть латиница + цифры + стандартные спецсимволы, кириллица/эмодзи/пробелы блокируются) и поле пароля переведено в контролируемый режим (`useState<string> passwordInput` + `value`/`onChange` на `TextField`), потому что **прямая мутация `e.target.value` в обработчике `onChange` не сработала** — react-aria `TextField` с `validate` держит собственное internal controlled-состояние (`useContextProps`/`mergeProps` на `Input` синхронизирует контекст `TextField`), и на следующий рендер значение поля откатывалось к нефильтрованному; после перехода на `value`/`onChange` на уровне `TextField` фильтрация стала настоящей (символ визуально не попадает в поле). Также обновлён текст `Description` пароля («Латиница, цифры и спецсимволы»). (3) Заодно найден и исправлен смежный баг: у email `TextField` был явный `isInvalid={Boolean(emailError)}`, который **полностью подавлял** собственную real-time валидацию поля через `validate` — из-за этого некорректный формат email никогда не показывался как `FieldError` (только серверная ошибка 409), хотя выглядело так, будто проверка работает. Пробовал включить `validationBehavior="aria"` на `Form` для показа `FieldError` из `validate` — отклонено: в `aria`-режиме ошибка «Пароль должен содержать не менее 8 символов» показывалась сразу при загрузке пустой формы, до любого взаимодействия пользователя (нарушает «не показывать ошибки до взаимодействия»). Вместо этого `validate` у email убран, проверка формата перенесена в ручной `onBlur` (пишет в тот же `emailError`, что и 409-кейс) плюс проверка перед сабмитом в `onSubmit` — оба источника ошибки email теперь идут через один и тот же управляемый `isInvalid`/`FieldError`. Проверено вживую через Playwright: ввод `Privet Мир123!😀` в пароль → в DOM остаётся `Privet123!` (пробел, кириллица и эмодзи вырезаны); email `user@example..com`/`not-an-email` — после `onBlur` показывается `[invalid]` + текст «Введите корректный email», исчезает при повторном вводе; полный сценарий (валидный email + `Valid Пароль_123!` → отфильтровано до `Valid_123!`) → успешная регистрация, редирect на `/`, тестовый пользователь удалён из БД после проверки. `tsc --noEmit`, `eslint` — чисто.
