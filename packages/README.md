# packages

Shared packages consumed by `apps/web` (and, over time, other apps in this monorepo).

Empty for now — add packages here as npm workspaces (`packages/<name>`) when code needs to be shared, e.g. UI components, types, or config presets.
